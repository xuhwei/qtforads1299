1. 储存路径优化，默认在当前目录下新建ZJUEEGDATA文件夹

2. 添加glazer评估
+ 启动，结束，glazer绘图摁键
+ 多线程处理glazer 秒表计数
+ 多线程处理glazer绘图（等待时间有点久）
+ 启动glazer评估将另外生成文本文档保存启动至结束的原始数据

3. 彻底去除希尔伯特求取包络

4. 添加通道快捷配置，快速使能通道

5. 引入FFT计算
+ FFT算法由matlab导出
+ 绘图由qcustomplot实现
*  修改FFT点数为1000，频谱分辨率为1Hz。这将导致FFT受采样率变化影响。不宜改变采样率。
*  预设32通道，更改通道数需修改较多内容。同IIR滤波器的缺陷。
*  时域频域非同时刷新操作。时域采用200个点刷新一次，频域采用1000个点刷新一次。

6. 优化FFT显示
+ 添加菜单栏FFT绘图配置选项卡，可选择自动坐标或设定坐标范围，默认为自动坐标。

7. 切比雪夫I型滤波器修改
+ 阶数降低，以提高算法收敛速度。* 引入边缘不够陡峭问题，即在截止频率附近的阻带衰减不足-60dB。
+ 通带波动衰减3dB->1dB。通带性能更好了。

//更新2019.5.14
1. STFT选项卡只能对不超过10s的数据进行分析。相当于不可使用。
//更新2019.5.15
1. 采样率修改功能
+ 采样率可修改，同步发送配置命令给板子
+ 采样率修改同时，修改带通与陷波滤波器配置，非1000hz取消高通滤波器使能。
*  仅连接上板子后可修改

2. 修改电极模式和发送命令两个功能为连上板子后可用

3. 修改tcpconnect 类参数，去除QWidget参数，该参数无用

4. 修复RMS相关说明

//更新2019.5.19
1. 滤波器对象存储与指针中；并修改高通为IIR库中滤波，使得采样率变更时，高通滤波器也可以用。
2. 修复FFT计算中内存释放问题
3. 更改SignalProcess中容器读取方式为.at(i).at(j)以提高速度
4. 修复myfame绘图中电极脱落更新速度
5. 修复TCP连接中重复删除指针，修复TCP中IP,Port修改后无法实际传入TCP的问题
x. 修改各个文件以适配通道数可改变场景。
+ tcpconnect.cpp 修改通道数为传入参数，修改读取包大小。
      ？这里暂未考虑通道数变化后电极脱落数据随着增多的情况。
+ signalprocess.cpp 修改滤波器存储方式，修改未容器存储和内存分配，适配通道数可更改。修改FFT通道数以适配通道数更改的场景。FFT绘线颜色32种循环。
+ myframe.cpp 于通道数无影响，关键在于myframe控件在ui界面上的增减问题。
+ mainwindow.cpp  修改中。

//2019.5.20~5.21
1. 修改通道数在窗口初始化时实现，通过弹出qDialog窗口确认通道数
+ 通道数最小32，以32递增，最大256通道
+ 由于FFT耗时，通道数超过32时，关闭FFT功能
+ channelset对话框中增加通道至256
+ 主界面中通道增加至256，由tab窗口切换，未启用的通道不使能。
2. 实测采样率更改无问题，存在250Hz采样率时刷新过慢的问题，可修改per_channel_number至100进行修改。

//2019.6.4
优化读取文件显示
